# Dependências do dashboard Streamlit hospedado (src/dashboard/app.py + src/dashboard/data.py).
#
# Gerado a partir do grafo de imports real (não de `pip freeze`). httpx/tenacity
# entraram no grafo quando o painel "Coletar leads" passou a importar
# scheduler/pipelines.py (que compõe os conectores de ingestão, ver
# ingestion/br_opencnpj/client.py e discovery.py). apscheduler, faker e
# python-dotenv continuam de fora — rodam só localmente/via CLI (scheduler real,
# seed sintético) e não são importados por este app — ver CLAUDE.md.
#
# Pins soltos (>=), sem pacotes específicos de SO (o host é Linux).

streamlit>=1.35
duckdb>=1.0
polars>=1.0
pydantic>=2.7
openpyxl>=3.1
httpx>=0.27
tenacity>=8.2
